# 프로젝트 목표

- 이 프로젝트의 목표는 실시간으로 렌더링되는 게임을 개발하는 것이다.
- 게임은 쿼터뷰 시점이며, 화면은 2.5D 모습으로 표현한다.
- 장르는 오픈월드 형태의 RPG이다.
- 싱글 플레이 게임이며, 시간적 배경은 미래이다.
- 분위기는 포스트 아포칼립스로 확정한다.
- 핵심 스토리는 주인공이 물리적 또는 심리적으로 망가진 세계를 탐험하며 희망을 찾는 내용이다.
- 2.5D의 구체적인 구현 방식(스프라이트 기반, 3D 기반, 혼합 등)은 아직 확정되지 않았다.
- 코드 분석과 설계 제안은 이 목표를 기준으로 하되, 구체적인 장르나 기능은 사용자가 정한 범위에 따른다.

# 게임 개발 기본 지침

- 최신 인벤토리/지도 조작은 E 전용이다. 인벤토리는 E로만 열고 닫으며 ESC는 무시한다. 지도 창에서 E는 지도만 닫고 인벤토리로 돌아간다. 지도에는 닫기/뒤로 버튼과 ESC 닫기를 두지 않는다. 지도 아이콘은 네 스킬 아이콘 오른쪽 남은 상태창 공간의 가운데에 배치한다.
- 지도 창 상단에는 획득 수(1/8)를 표시하지 않는다. 첫 지도 조각은 챕터 1 배치를 종이색에 어울리는 폐허·거점·창고·길 스케치로 그린다. 첫 목적지의 집 아이콘과 '다음 목적지' 문구는 제거한다. 첫 조각에서 마지막 목적지 일부와 그 위의 '우리의 집'이 보인다. 설명은 실제 글꼴 너비로 줄바꿈한다. 상세: outputs/지도창_조작_삽화_개정_안내.txt.

- 최신 챕터 1 진행·지도 UI는 outputs/챕터1_진행_지도조각_개정_안내.txt를 우선한다. 바리게이트는 7개, 출발 지점은 (1.6, 25.5)이다. 소리 확인 및 지도 조각 획득 전에는 거점 입구를 막고 독백을 표시한다. 귀환 후 물자를 먼저 보관해야 장미와 대화할 수 있다.
- 미니맵은 맵의 지형·건물·배치물을 간략히 표시하고 주요 대상을 강조한다. 챕터 1 스토리 목표의 경로 방향은 여우가 바라보는 쪽의 발 앞 바닥에 삼각형 화살표로 표시한다.
- 지도는 일반/스토리 슬롯에 넣지 않고 인벤토리 스킬 줄 오른쪽 아이콘으로 전용 창을 연다. 8비트 획득 상태로 조각을 구분하며 이름은 1개 '지도 조각', 2~7개 '지도 조각들', 8개 '지도'다. 현재는 첫 조각만 지급하고 다음 목적지에 관한 일부만 보여 준다. 조각 클릭 시 설명 표시, ESC로 단계별 뒤로, E로 전체 인벤토리를 닫는다. 지도 문구를 대사에서 인용할 때 「우리의 집」을 사용한다.

- 챕터 선택 제목 배율은 1.25, 설명은 0.78이다. 챕터 1 설명은 "평범한 일상을 보내던 여우는 어느 날," 다음 줄에 "특이한 존재와 만나게 된다."로 표시한다. 렌더링 최적화 내용과 검증 범위는 outputs/챕터_UI_렌더링_최적화_안내.txt를 참고한다.

- 챕터 소스 이름은 Chapter1.cpp/.h, Chapter1Story.cpp, Chapter1Environment.cpp, Chapter2.cpp/.h, Chapter2Render.cpp로 통일한다. 클래스는 Chapter1/Chapter2다. 이전 문서의 옛 파일명은 outputs/챕터_파일명_통일_안내.txt 대응표를 따른다.

- 챕터 1 모델·가림 처리는 outputs/챕터1_모델_가림_수정_안내.txt를 참고한다. 긴 벽은 연속 패널로 나누고 모서리 중복 면을 피한다. 건물과 캐릭터의 가림은 바닥 범위 기반 순서로 결정하며 개별 벽 자동 투명화는 하지 않는다. 이전 거점 좌표의 천막·물자 장식은 제거했다.

- 인벤토리는 상단 20칸 일반 아이템, 하단 10칸 스토리 아이템으로 구분하며 영역 사이에 추가 간격을 둔다. 식료품·생필품·지도는 스토리 슬롯에 표시한다. 챕터 1 바닥은 검은 테두리 대신 균열·얼룩으로 다양화한다. 챕터 소개는 '이상한 존재'로 표기한다.

- 챕터 1 임시 설명 UI(시작·획득 토스트, 상호작용 대상 설명, 소리·귀환·출발 텍스트 표식)는 사용자 후속 지정 전까지 숨긴다. 스토리 대화와 최초 컨트롤 안내는 유지한다. 지도 패널은 220×204다.

- 컨트롤 안내창은 대화창 바로 위에 공통 형태로 표시한다. 첫 대화 페이지의 안내는 별도 창의 'LMB로 대화창 넘기기'다. 챕터 1 제목은 '모험의 시작', 소개는 '평범한 일상을 보내던 여우는 어느 날, 특이한 존재와 만나게 된다.'다.

- 최신 챕터 1 조작 안내는 outputs/챕터1_최초조작_안내.txt를 우선한다. 안내는 최초 기능 사용까지 표시하고 완료 후 재표시하지 않는다. 첫 대화 페이지만 넘기기 안내, 도입 후 이동 안내, 첫 물자 접근 시 F 안내, 첫 획득 후 E 안내를 표시한다. 획득 전 인벤토리는 잠긴다. 챕터 1 HP 및 지도 아래 퀘스트 안내는 숨긴다. 지도 제목은 현재 맵 이름 '여우와 장미의 거점'이다.

- 거점 바리게이트는 총 5개, 모험 출발 지점은 북쪽 골목 안쪽 (2.5, 25.5)이다. 장미의 첫 대사는 사용자 지정 표기 '바리게이트'를 사용한다. 독백 화자명은 '여우'만 표시하고 작은따옴표로 독백을 구분한다. 과거 독백 접미사 표기를 대체한다.

- 최신 대사·HUD·진행은 outputs/챕터1_UI_진행_개정_안내.txt를 참고한다. 일반 대사는 큰따옴표, 독백은 작은따옴표로 감싸며 지문은 화자명·따옴표 없이 표시한다. 지도는 왼쪽 위, HP는 오른쪽 위, 현재 목표는 지도 아래다. 챕터 1 소리 이벤트는 수집 완료 후 식료품점 출입구에서 발생한다. 시작 시 1.2초 페이드인을 적용한다.

- 챕터 1 모험 출발 지점은 거점과 바로 북쪽 건물 사이 골목 (6, 26)이다. 대화 본문 배율 0.95, 화자 1.1, 줄 높이 28을 사용하며 긴 대사에 맞춰 창 높이를 조절한다.

- 최신 챕터 1 대사는 outputs/챕터1_대사_개정_2026-09-17.txt를 우선한다. 괄호 대사는 여우의 독백이며 행동 설명은 지문이다. 지도 문구는 '우리의 집'이다. 물자 수집 완료 대사와 귀환 중 소리 대화를 분리하고 크리처의 마지막 침묵에서 사망을 적용한다.

- 챕터 1 환경은 outputs/챕터1_환경_디테일_안내.txt를 참고한다. 건물은 높이를 달리하고 뒤를 가리며 깨진 창문과 덩굴을 표시한다. 상자 더미는 높낮이·색·각도를 달리한다. 거점에 생활 가구·바리케이드·작은 셰이더 모닥불을 둔다. 공격형 크리처는 경계·추격·공격 중 붉은 눈을 표시한다.

- 최신 챕터 1 맵은 outputs/챕터1_맵_재배치_안내.txt를 우선한다. 남서 거점, 북서 생필품, 북쪽 철창의 크리처 2마리, 동쪽 식료품 창고, 남동쪽 상자 뒤 말하는 크리처로 배치한다. 기존 강제 관찰 통로를 제거하고 두 물자 수집 후 수집 지점을 벗어나면 소리 추적을 시작한다.

- 프로젝트 이름과 폴더는 FoxAndRose이며 솔루션은 FoxAndRose.sln이다. 진입 소스는 FoxAndRose/FoxAndRose.cpp다. 과거 문서의 SimpleGame 파일 경로는 FoxAndRose로 읽는다. 게임 표시 제목과 출력 이름 Fox And Rose는 유지한다.

- 표지 별은 총 7개이며 달은 윗끝 높이를 유지하면서 축소해 건물과 간격을 둔다. 페이지 모서리는 안쪽으로 뒤집힌 종이 형태이며 뒤집힌 삼각형 부분만 클릭·호버에 반응한다.

- 최신 공격형 크리처는 평소 이족으로 배회하다가 추격·공격 시 사족으로 전환한다. 기계 융합 조직은 팔뚝·정강이·얼굴 일부에만 남긴다. 기존 사족 기본 및 주기적 기립 지침을 대체한다. 모델 캐시 버전은 6이다.

- 최신 공격형 크리처는 거대한 유인원 같은 체형으로 네 발 보행을 기본으로 종종 두 발로 걷는다. 신체의 기계 조직은 진화 과정에서 기계와 일부 융합한 결과이며 삽입 장비가 아니다. 인간 유래 설정을 유지한다. 여러 종류 중 공격적인 종류에만 적용하며 말하는 크리처는 이번 외형 변경에서 제외한다. 모델 캐시 버전 5, 상세 내용은 outputs/공격형_크리처_외형_구현_안내.txt를 참고한다.

- 최신 여우 모델은 얼굴과 연결된 귀, 정면·측면의 흰 주둥이와 검은 코, 장미와 같은 형태의 호박색 눈을 사용한다. 연두색 상의와 방향에 맞는 옆모습 몸통·팔다리를 적용한다. 캐릭터 아틀라스 버전은 7이다. 과거의 눈만 표현한다는 지침보다 우선한다.

- 최신 제목은 여우와 장미, 영어명 Fox And Rose, 약칭 FAR이다. 표지에는 한국어 제목을 표시하고 프로그램 창과 빌드 출력 이름은 Fox And Rose를 사용한다. 이전 제목 지침보다 우선한다.
- 여우 인게임 모델은 모자를 쓰지 않는다. 머리카락/꽁지머리는 계속 숨긴다. 캐릭터 아틀라스 버전 7로 기존 모자 캐시를 갱신한다. 인벤토리 왼쪽 캐릭터는 어두운 실루엣 대신 원래 색상의 모델을 표시한다.
- 표지 여우의 두 앞다리는 안쪽으로 기울여 앞발 사이에 작은 간격만 남긴다. 인게임 다리와는 별도다.

- 장미 NPC는 검은 고양이 수인으로 후드티와 트레이닝 바지를 입는다. 현재 후드티 가슴의 장미 자수 문양으로 장미 컨셉을 표현한다. 여우와 같은 표시 배율의 세분화된 도형 모델을 사용하며 Renderer::Model::Rose로 파일 캐싱한다.

- 최신 챕터 1은 outputs/챕터1_지도와출발_구현_안내.txt를 우선한다. 거점 내부에서 검은 고양이 수인 장미와 물자 대화 → 두 물자 회수 → 귀환 통로에서 소리 → 아프다고 말하고 죽는 크리처 → 약도와 흐릿한 '우리들의 집' 종이 발견 → 거점 귀환·장미에게 모험 제안 → 거절과 심한 말로 다툼 → 화난 여우의 단독 출발로 진행한다.
- 기존의 NPC 없는 거점, 시작 독백, 물자 보관 후 소리 추적 지침은 위 흐름으로 대체한다. 장미 NPC와 통로의 별도 죽어가는 크리처를 구현했다. 철창 안의 관찰용 크리처 두 마리는 유지한다.
- 지도 조각은 인벤토리에 표시되며 챕터 1 출발 후 연결된 챕터 2에 유지된다. 챕터 2 직접 시작에는 지급하지 않는다. 챕터 2의 후속 서사 및 왕 관련 내용은 아직 추가하지 않는다. 왕자의 정체, 지도 목적지의 정체는 미정이다.

- 게임 제목은 붉은 여우와 검은 장미로 확정한다. 표지의 검은 장미 꽃은 상징적 삽화이며, 등장인물 장미는 검은 고양이 수인 설정을 유지한다.

- 메인화면은 확정 제목 붉은 여우와 검은 장미·종료 버튼과 폐허·식물·붉은 여우·검은 장미 꽃 삽화를 담은 닫힌 책이다. 종료 버튼 이외의 표지를 클릭하면 책이 열리며 챕터 선택으로 이동한다. 별도 게임 시작 버튼은 표지에 두지 않는다.
- 챕터 선택은 펼친 책의 양쪽에 한 챕터씩 배치한다(1–2 / 3–4 / 5–6 / 7–8). 각 페이지에는 번호·제목·짧은 소개가 있다. 하단 접힌 모서리로 이전/다음 펼침면을 넘기는 모션을 재생한다. 첫 펼침면에서 이전을 누르면 책을 닫고, 마지막에는 다음 버튼을 표시하지 않는다. 수집 페이지는 아직 추가하지 않는다.
- 메뉴 챕터 1은 기존 Chapter1, 챕터 2는 기존 Chapter2으로 연결한다. 기존 레벨 1이라는 구현 명칭은 유지하되 사용자에게는 챕터 2로 표시한다. 챕터 2의 왕 관련 서사는 아직 구현하지 않는다. 챕터 3~8은 페이지 열람만 가능하고 시작은 준비 중이다. 챕터 1·8의 제목과 소개 문구는 임시 UI 문안이다.
- 책 메뉴 구현은 BookMenu.h/cpp, 상세 동작과 사용자 확인 항목은 outputs/책_메뉴_챕터선택_구현_안내.txt를 참고한다.

- 전체 스토리는 8개 챕터로 확정한다. 챕터 1은 현재 튜토리얼을 기반으로 이상한 크리처를 만나 모험을 떠나는 부분까지다. 챕터 2~7은 순서대로 왕·허영심 많은 사람·술꾼·사업가·가로등지기·지리학자를 만난다. 각 챕터의 상세 이야기와 전투/대화 여부는 미정이다. 챕터 8은 종착지에서 수많은 장미와 왕자를 만나 깨달음을 얻고 자신의 장미에게 돌아가는 엔딩이다.
- 스토리의 '챕터 1'과 기존 '레벨 1' 시스템 프로토타입은 구분한다. 챕터 구성 확정은 코드 구현 완료를 뜻하지 않는다.

- 최신 스토리(2026-09-16)는 `outputs/스토리_개정안_2026-09-16.txt`를 우선한다. 여우·장미·왕자는 확정 이름이 아닌 역할 명칭이다. 어린 왕자에서 모티브를 얻되 여행하는 여우와 깨달음을 전하는 왕자로 역할을 반전한다.
- 여우는 장미와 거점에서 살며 반복되는 일상과 까칠한 장미에게 지쳐 특별한 경험을 원한다. 물자 탐색 중 말을 하는 크리처에게 미정의 사실을 듣고 장미를 두고 떠난다. 6개 챕터에서 왕·허영심 많은 사람·술꾼·사업가·가로등지기·지리학자 모티브의 보스를 만나고, 마지막 챕터에서 수많은 장미와 왕자를 만난 뒤 깨달음을 얻어 자기 장미에게 돌아간다.
- 여우·장미 등 플레이어 캐릭터와 일반 NPC는 수인이다. 장미는 검은 고양이 수인으로 확정한다. 인간 유래 크리처는 몬스터에 해당하는 존재로 등장한다.
- 장미와 여우는 현실의 초등학교 저학년 정도의 어린 시절에 만난 고아·떠돌이 친구로 지금까지 함께 지냈다. 종의 습성·성격·의견 차이로 자주 다퉜다. 여우는 생계를 위한 반복되는 일상에 지루함을, 장미는 변화 없는 평온함에 만족을 느낀다. 여우의 모험 결심에 장미가 위험을 무릅쓴다며 평소보다 심한 말을 하고, 화난 여우는 혼자 떠난다.
- 헤어진 뒤 장미는 평온한 일상에서, 여우는 새로운 경험 속에서 허전함을 느낀다. 여우는 여행 중 친구들을 사귀고 종착지의 작은 수인 마을에서 잠시 지내도 허전함이 남는다. 왕자는 장미가 소중한 이유가 유일한 친구여서가 아니라 함께 지내며 관계를 쌓은 시간에 있음을 전하며, 여우는 귀환을 결심한다. 재회 대화와 이후 관계는 미정이다.
- 왕자의 정체는 미정이다. 평범한 모험가 같은 인물이면 수인, 처음 발견한 이상한 크리처 본인이면 수인이 아닌 평범한 인간의 모습으로 표현한다. 후자의 외형이 나타나는 원리나 시점은 미정이며 인간 생존·복원·변신을 임의로 확정하지 않는다.
- 크리처가 전하는 사실, 그 크리처와 왕자가 동일 존재인지, 보스전 방식은 미정이다. 인간 멸종과 진화한 동물 세계 설정은 유지한다.
- 이번 스토리 변경은 기획에 반영하며 기존 튜토리얼 코드의 NPC 없는 거점·물자 보관 후 크리처 만남은 아직 변경하지 않았다. 서사 도입 순서와 장미의 구현은 후속 작업에서 맞춘다. 레벨 1의 시스템 프로토타입에 스토리를 자동 추가하지 않는다.

- 최신 상태창은 'LV 숫자 + 경험치 바'와 한 줄의 'MHP 숫자 / ATK 숫자 / DFS 숫자'로 표시한다. 아래에는 샘플 스킬 4개(첫 번째만 해금), 그 아래에는 효과 아이콘을 배치한다. 잠긴 스킬은 어둡게 표시하고 자물쇠를 그린다.
- 샘플 스킬 툴팁은 스킬1~스킬4다. 샘플 효과는 고정 시간 '10:00'과 설명 '튜토리얼 중'을 툴팁으로 표시한다. 샘플은 UI 시연용이며 실제 스킬 실행이나 시간 감소는 없다. 적용 효과가 없다는 문구는 표시하지 않는다.

- 최신 인벤토리는 청회색 불투명 배경과 밝은 테두리를 사용하며 InventoryTheme으로 색을 분리한다. 좌측 제목은 '스테이터스', 우측 제목은 '인벤토리'다. 소지품이 있는 칸에 마우스를 올리면 이름을 표시한다.
- 상태창은 레벨 숫자와 다음 레벨 경험치 바를 한 줄에 표시한다. 능력치는 최대 체력·공격력·방어력만 표시한다. 피해량은 공통 공격력의 근접 100%, 원거리 50%(소수점 버림)로 계산한다. 별도 투척 공격력 능력치는 사용하지 않는다. 이전 인벤토리/능력치 지침과 충돌하면 이 항목을 우선한다.

- 아래쪽을 보는 주인공은 꼬리를 그리지 않는다. 좌우 시점의 귀는 가까운 귀와 작은 먼 귀를 구분하고, 위쪽 시점에서는 귀 안쪽 대신 바깥 털과 뾰족한 검은 끝을 표현한다.

- 튜토리얼과 레벨 1의 E 인벤토리는 왼쪽 실루엣·레벨·누적 경험치·공격력·투척 공격력·방어력·효과, 오른쪽 가로 5칸×세로 6칸 슬롯의 소지품 아이콘과 수량을 표시한다. 왼쪽에는 종족명과 현재 HP를 표시하지 않는다. 열린 동안 게임을 정지하고 E/ESC로 닫는다.
- 상시 캐릭터 UI는 왼쪽 위 HP, 오른쪽 위 지도와 현재 위치만 표시한다. 획득·레벨 상승 알림, 대화 및 상호작용 안내는 상황에 따라 표시한다.
- 레벨 1에는 불·물을 배치하지 않는다. 불꽃·물결 셰이더는 향후 해당 환경이 등장할 때 활용할 기반으로만 남긴다.
- 파이프 공격은 반투명 잔상을 표시한다. 몬스터 피격 시 충돌을 유지하는 짧은 넉백·경직과 방사형 타격 이펙트를 적용한다.
- 주인공 후면 꼬리는 허리 아래 중앙에서 연결되고 몸통·다리 뒤에 가려지지 않게 그린다. 좌우 얼굴은 짧게 돌출된 여우 주둥이를 표현하되 입 등의 세부 묘사는 생략한다.
- 레벨 1 쇠파이프는 모델 길이 39(기존 29), 휘두름 180도, 근접 판정 거리 2.3m를 적용한다. 외형 변경 시 디스크 캐시 버전도 갱신한다.

- 레벨 1은 튜토리얼과 별도인 맵·RPG 시스템 프로토타입이다. 말하는 크리처의 후속 스토리는 넣지 않는다.
- 레벨 1은 좌클릭 쇠파이프 근접 공격, 우클릭 돌 투척, 아이템 획득, 누적 경험치 기반 캐릭터 레벨 및 HP·공격력·방어력 조절을 포함한다. 대쉬·스캔은 보류한다.
- 랜덤 맵은 모든 이동 가능한 바닥의 연결성을 보장한다. 장식·아이템으로 통로를 막지 않는다.
- 절차적 모델은 최초 생성 후 버전이 있는 파일 캐시로 저장하고 다음 실행에서는 로드한다. 불꽃·물결은 시간 기반 셰이더로 표현한다.
- 현재 구현 범위, 수치 및 남은 작업은 `outputs/레벨1_시스템_구현_안내.txt`를 참고한다.

- 최신 확정 세계관: 인류가 욕망으로 일으킨 대전쟁 이후 살아남은 동물이 번성하고 인간과 비슷한 외형·지능으로 진화했다. 기존 인간은 모두 사라졌으며 인간에서 비롯된 크리처만 남았다.
- 주인공은 진화한 동물의 세계에서 살며, 튜토리얼에서 물자를 전부 거점에 보관한 뒤 주변의 소리를 따라가 더듬더듬 말하는 특이한 크리처를 발견한다. 일반 크리처는 말하지 못한다. 현재 구현 안내는 `outputs/주인공_외형_소리추적_구현_안내.txt`를 참고한다.
- 주인공은 붉은 여우에서 진화한 여우 수인으로 귀와 꼬리가 있다. 민첩함은 대쉬, 뛰어난 후각·청각은 스캔 기능으로 반영할 기획이다. 구현 완료를 뜻하지 않는다.
- 외형은 붉은 여우색 귀·꼬리(끝에 약간의 검정), 스키복 같은 오버핏 의상, 붉은 목도리다. 꽁지머리는 설정에만 남기고 인게임에서 숨긴다. 파일럿 모자로 머리를 가리고 여우 얼굴에는 눈만 표현한다. 꼬리는 끝이 뾰족한 타원형으로 표현한다. 현재는 원거리 쿼터뷰 실루엣을 우선한다. `outputs/주인공_설정.txt`를 참고한다.
- 주인공 이외의 생존 동물 종, 변화의 원리·기간, 발견 이후의 목표와 엔딩은 미정이다. `outputs/세계관_스토리_목표_제안.txt`의 확정 사항과 제안을 구분한다. 기존 인간 생존자 중심 스토리 초안보다 최신 확정 세계관을 우선한다.

- 현재 프로토타입은 5분 이내 튜토리얼 레벨이다. WASD 이동, F 상호작용, 쿼터뷰 2.5D를 사용한다.
- 식물이 무성한 폐허 건물에서 시작해 거점 근처 식료품·생필품을 회수하는 간단한 퀘스트를 포함한다.
- 전체 분위기를 표현하는 렌더링 결과물이 필요하다. 생성 콘셉트 이미지를 실제 엔진 결과로 간주하지 않는다.
- `outputs/튜토리얼_레벨_설계서.txt`에서 현재 레벨 범위와 제안 사양을 참고한다.
- 후속 개선 범위에 사후 처리·그림자·캐릭터 애니메이션·머티리얼·환경 효과와 거점 밖 크리처를 포함한다.
- 시작 독백은 마우스 좌클릭으로 넘기며, 독백 종료 시 물자 퀘스트를 자동 획득한다.
- 거점에는 NPC 없이 주인공만 있으며, 회수한 물자는 거점 보관함에 넣는다.
- 크리처 2마리는 필수 물자 동선의 관찰 통로 옆에 있다. 주인공 발견 시 차단벽 쪽으로 접근하지만 서로 넘어가거나 공격할 수 없다.
- 최신 변경 사항은 `outputs/메인화면_일시정지_필수관찰통로_안내.txt`를 참고한다.

- `outputs/게임_개발_지침.txt`를 지속적인 기획 및 개발 기준으로 참고한다.
- 문서의 확정 사항은 기본 틀로 유지하고, 메인 퀘스트 트리와 세계관 세부 사항은 제안 초안으로 구분한다.
- 사용자가 변경을 요청하지 않는 한 확정된 기본 방향을 임의로 바꾸지 않는다.

# 사용자 작업 선호

- 코드 작성 및 수정 시 루트 `.clang-format`을 따른다. C++ 및 GLSL 소스는 공백 4칸 들여쓰기, Allman 중괄호, 100열 기준으로 정리한다.
- 짧은 함수·조건문·반복문도 본문을 별도 줄에 작성한다. 서로 다른 실행문을 한 줄에 몰아 쓰지 않는다.
- 함수·타입 정의 사이와 논리적으로 다른 처리 단계 사이에는 빈 줄을 넣는다. 연속 빈 줄은 최대 1줄로 유지한다.
- 기존 식별자 명명 방식(타입·함수 PascalCase, 지역 변수 camelCase, 멤버 m_ 접두사)을 유지한다. 포맷 정리만을 위해 식별자나 동작을 변경하지 않는다.
- 포맷 적용은 직접 관리하는 소스에 한정한다. Dependencies, 빌드 산출물, 배포용 복사본은 제외한다. 앞으로 수정 후 해당 파일의 포맷을 확인한다.

- 특별한 상황이 아니라면 빌드와 실행 결과 확인은 사용자가 직접 수행한다.
- 기본적으로 코드 분석, 정적 검토, 수정까지만 수행하고 빌드나 실행 검증을 자동으로 진행하지 않는다.
- 사용자가 명시적으로 요청하거나 예외적으로 필요한 경우에만 빌드 및 실행 검증을 수행한다. 예외가 필요한 경우 그 이유를 먼저 설명한다.
- 완료 보고에는 직접 검증한 범위와 사용자가 확인할 사항을 구분해서 적는다.

- 실행 시 제목·게임 종료 버튼이 있는 닫힌 책 표지로 진입하고, 표지를 클릭해 챕터 선택을 연다. ESC로 일시정지 팝업을 열거나 닫고 팝업에서 메인화면으로 돌아간다.
- R 재시작과 세이프존 개념 및 표시는 제거한다. 거점은 벽과 제한된 출입구로 둘러싼다.
